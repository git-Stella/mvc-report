{% extends "base.html.twig" %}

{% block title %}Card Game{% endblock %}

{% block body %}
<h1>Card game</h1>

<p>Card games are a classic.</p>
{{ include('flash.html.twig') }}

<h1>Hand</h1>
<ul>
    {% for card in hand %}
        <li>{{ card }}</li>
    {% endfor %}
</ul>

<ul>
    {% for card in phand %}
        <li>{{ card }}</li>
    {% endfor %}
</ul>

<form action="{{ path('game_final') }}">
    <button type="submit" value="Val">Resultat</button>
</form>

{% endblock %}
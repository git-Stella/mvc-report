{% extends "projbase.html.twig" %}

{% block title %}Home{% endblock %}

{% block body %}
    <h1>Poker play casino</h1>
    {% for i in range(0, deck|length-1) %}
        {{ deck[i] }}
    {% endfor %}
    <h2><a href="{{ path('projstate') }}">Begin playing</a><h2>

{% endblock %}